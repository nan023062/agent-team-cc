// Placeholder — v1 → v2 migration command
// Phase 0 will implement: plan + apply via @cbim/engine/migration

export function migrate(projectPath: string): void {
  console.log(`cbim migrate: not implemented yet (target: ${projectPath})`)
}
