@
---
tier: short
tags: session pending-decision
modules: cbi/agents memory/engine cbi/_primitives
---

## Task Overview

本次 session（2026-05-22）完整推进了 CBIM 从 v1.0.0 → v1.0.5 的五个版本，全部已 push origin master 并打 tag。下个会话需接续 1.0.6 = task-14 archive-distill 三子项的 scope 决策与执行。

## Session History

- v1.0.1 — 主执行流 P0/P1/P2（DNA 四态判定 + ContextPack schema + dispatch skill 三补 + soul-prompt 自治循环 + 全部写入 kernel templates）
- v1.0.2 — fix cbim migrate PYTHONPATH bug（updater 反向 import cbim_kernel 违反 .dna 铁律；修法：把 sync 实现搬到 updater/sync.py）
- v1.0.3 — 治理循环 P0（HR 死结解：agent update / add-skill CLI + SessionStart 阈值 banner + memory.distill.* 配置项）
- v1.0.4 — 治理循环 P1（writer signals 模板 + 14 处 skill CLI 漂移对齐 6 处编辑）
- v1.0.5 — DNA P2-1/P2-2（status frontmatter 字段 spec/planned/implemented + cbim dna split 原子命令；Branch A loopback 首次真实闭环：programmer 触发 NEEDS_ARCH_DECISION → architect mini-triage 选 b deprecation 是 lifecycle 轴 → programmer 续派应用决定）

## Pending 1.0.6 — task-14 family

Architect 已产 3 个 ContextPack（agentId: a474bda7bc79c4ecc），三组契约：
- 契约 A（14a 产出，14c 消费）: memory entry frontmatter agent-id list 字段（main + 调度过的 subagent_type）+ cbim memory query --agent flag
- 契约 B（14b 产出，14c 消费）: sentinel 文件 .cbim/memory/_pending_distill/<id>.json schema（agent_id / archived_at / trigger / short_entry_paths）
- 契约 C（14c 实现）: cbim memory distill-agent <id> 命令 — 消费 sentinel + LLM 蒸馏 → medium tier，5 种失败路径状态机

Task IDs in tracker: #14（伞）/ #16 (14a) / #17 (14b) / #18 (14c)，均 in_progress。

## User Decision Pending

14c 复杂度警告：~400-600 行实现 + 200 测试（比 14a/14b 合起来还大），含 LLM 调用 + 新 prompt 设计 + medium tier 首次写入 + 5 种失败路径。

三个选项交用户拍板：
- (a) 三项全做 1.0.6（架构师默认）—— 14a/14b 并行先合，14c 单独 PR 最后串行
- (b) 1.0.6 = 14a + 14b 双 PR，14c 推 1.0.7
- (c) 1.0.6 = 只 14a，14b/14c 留下个版本

权衡：14a/14b 没 14c 是"写得出但没人读"，本轮一定要做 14c 才算闭环；但 14c 是 hairy ball。

## Other Open Follow-ups

- Task #6: dev-install docs（updater 重装路径 surface 到 README/help —— cbim install --local v1/src/kernel 同时刷 sibling packages 的事实未文档化）
- Task #15: lifecycle frontmatter field schema（v1.0.5 Branch A 产生：deprecation 不进 status enum，独立 lifecycle 轴待设计）
- 治理循环 P2 剩余：archive 时 agent 经验自动入 medium（task-14 family 这一批解决）
- memory_distill skill 双语 heading 漂移收口（architect P1-1 报告里点过，全局统一中文 / 英文 heading 的决策）

## How to Resume

下次 session 用户说"继续 1.0.6"或类似措辞 → 直接复读 ContextPack（不在 .cbim/memory 里存，在 architect 的会话历史里 lost）→ **必须重新派 architect 再 triage 一次**（agentIds 跨 session 不复活）→ 再让用户拍 a/b/c → 派 programmer 执行。

更省力路径：让用户先拍 a/b/c，然后告诉 architect "重新产 task-16/17/(18) 的 ContextPack，参考 v1.0.5 的契约设计，模式同 v1.0.6 上次"，架构师可从 git log + CHANGELOG + 已合并代码状态恢复上下文。

## Files Touched This Session

太多不列。git log 0eec57d..1dc2ccb 五个 release commit 涵盖全部。

## Signals

- [x] IS: cbim-kernel: v1.0.5 已 push origin master，1.0.6 = task-14 family pending user scope decision (a/b/c)
- [x] HOW: coordinator: 跨 session 续派必须重新走 architect 必经门——subagent 会话历史不复活；可用"参考 git log + CHANGELOG"让 architect 快速恢复上下文
- [x] HOW: coordinator: release 流程稳定模式 = architect triage → 并行 programmer → architect release prep（VERSION+CHANGELOG+README+.dna 决定）→ install --local → migrate → smoke test → commit + tag + push --follow-tags
- [x] WANT: kernel: 两批分发（小步快跑，子系统不混塞）是落地后验证有效的模式；1.0.5 = DNA / 1.0.6 = memory+agent 是首次实践，结果干净
- [x] WANT: kernel: Branch A loopback（NEEDS_ARCH_DECISION 回炉）在 v1.0.5 首次真实场景闭环，证明 soul-prompt 自治循环在 Sonnet 上能跑通；继续坚持 programmer 不自决的纪律
- [x] MUST: architect: 抽象层下沉是有意识的决定，不要被"加字段加原语"的诱惑顺手带入 .dna 合约（v1.0.4/v1.0.5 三次拒绝把新字段刻入 .dna，理由：.dna 描述粒度在族/类/方法签名层，不枚举字段/子动词）
@