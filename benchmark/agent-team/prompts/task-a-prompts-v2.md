# Task A 提示词 v2（纯自然语言，无文件路径）

---

## Turn 1

调度系统上加一个试运行模式：分析执行计划但不实际执行任何任务，返回一份报告，包含阶段数、任务数、预计总执行次数（含重试）、以及检测到的结构性问题（比如某任务依赖了不存在的任务）。

---

## Turn 2

预计总执行次数的计算要准确——如果默认配置下一个任务失败会重试几次，那 10 个任务的总执行次数应该是多少？把推导过程告诉我。

---

## Turn 3

好，按这个逻辑实现 dryRun，把接口定义和方法实现都写进去。
